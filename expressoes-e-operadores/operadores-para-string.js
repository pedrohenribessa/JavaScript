// Operador de String (String Operator)

// comparison (comparação)
console.log('a' == 'a')
// concatenation (contatenação)
// Retorna a união de duas strigns
let alpha = 'alpha'
console.log(alpha + 'bet' + 's')